{
  "mcpServers": {
    "torchtalk": {
      "command": "torchtalk",
      "args": ["mcp-serve", "--harness", "pytorch"],
      "env": {
        "PYTORCH_SOURCE": "${PYTORCH_SOURCE:-}"
      }
    }
  }
}
